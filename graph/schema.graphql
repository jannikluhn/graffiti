type Graffiti @entity {
  id: ID!
  pixels: Bytes!
}

type Pixel @entity {
  id: ID!
  owner: Bytes!
  price: BigInt!
  color: Int!
  earmarkedReceiver: Bytes!
}

type Account @entity {
  id: ID!
  recordedBalance: BigInt!
  lastTaxPayment: Int!
  taxBase: BigInt!
}